from odoo import models, fields, api, _


class ProductProduct(models.Model):
    _inherit = 'product.template'

    # Espacio para campos de 'control' a futuro.
    # Carlos Rizo menciona que pestania se queda pero campos de Malena no.